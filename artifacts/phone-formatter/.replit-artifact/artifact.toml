kind = "web"
previewPath = "/"
title = "Phone Number Formatter"
version = "1.0.0"
id = "artifacts/phone-formatter"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24479

[services.development]
run = "pnpm --filter @workspace/phone-formatter run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/phone-formatter", "run", "build" ]
publicDir = "artifacts/phone-formatter/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24479"
BASE_PATH = "/"
